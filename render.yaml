services:
  - type: web
    name: monpocatw
    env: node
    buildCommand: npm install && npm run build
    startCommand: npx serve -s dist -l 3000 -c serve.json
    envVars:
      - key: NODE_ENV
        value: production
